// Will display albums the user can play and when user 
// searches something this component will display the 
// search results from it

// For search, implement three functions in backend, 
// one to search in artists, one to search in albums 
// and one to search in tracks and then call those 
// three functions from frontend

import React from 'react'

const Explore = () => {
  return (
    <div>Explore</div>
  )
}

export default Explore